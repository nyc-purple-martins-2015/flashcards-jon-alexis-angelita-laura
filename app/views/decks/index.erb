<div>
  <% if logged_in?  %>

    <div class="user-greeting">
    <h1>Welcome back <%= session[:user_name] %>! </h1>
    <br>
    </div>

    <% else %>
    <h2> Welcome, please <a href = "/signup"> sign up </a> to play. </h2>
    <% end %>
</div>


<div id="deck-menu">
  <h1 class="user-greeting">My Decks:</h1>
      <% @decks.each do |deck| %>
      <div>
          <p><a class="deck-display" href="/decks/<%=deck.id %>"><%= deck.category %></a></p>
        <br>
      </div>
      <% end %>
</div>
